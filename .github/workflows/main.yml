name: Build Flutter AAB

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

# Добавляем принудительное использование Node 24, как просит GitHub
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5 # Обновили до v5

      - name: Install Java
        uses: actions/setup-java@v5 # Обновили до v5
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Install Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Decode Keystore
        run: echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 --decode > android/app/upload-keystore.jks

      - name: Get Dependencies
        run: flutter pub get

      - name: Build AAB
        run: |
          flutter build appbundle --release \
            --build-name=1.0.0 \
            --build-number=1 \
            --signing-key-store-path=upload-keystore.jks \
            --signing-key-store-password=${{ secrets.KEYSTORE_PASSWORD }} \
            --signing-key-alias=${{ secrets.KEY_ALIAS }} \
            --signing-key-password=${{ secrets.KEY_PASSWORD }}

      - name: Upload Result
        uses: actions/upload-artifact@v5 # Обновили до v5
        with:
          name: release-aab
          path: build/app/outputs/bundle/release/app-release.aab
